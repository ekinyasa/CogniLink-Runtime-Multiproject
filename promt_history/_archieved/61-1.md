Fix resolver crash when params.path is not a string.

File:
functions/campaign/[[path]].js

Cloudflare Pages catch-all params may return:

string
or
array

Current code assumes string and uses split(), which throws:

TypeError: path.split is not a function

Implement safe parsing.

Replace path parsing with:

const raw = context.params?.path

let segments

if (Array.isArray(raw)) {
  segments = raw
} else if (typeof raw === "string") {
  segments = raw.split("/").filter(Boolean)
} else {
  segments = []
}

if (segments.length !== 2) {
  return new Response("invalid route", { status: 400 })
}

const [alias, linkId] = segments

Do not change any other resolver logic.

---

## Implementation Summary (Prompt 61-1)

### Modified file: `functions/campaign/[[path]].js`

**Single change — safe path parsing:**

```js
// Before (P61): assumed string, threw TypeError on array
const path     = params?.path ?? "";
const segments = path.split("/").filter(Boolean);

// After (P61-1): handles string | array | undefined
const raw = context.params?.path;
let segments;
if (Array.isArray(raw))        { segments = raw; }
else if (typeof raw === "string") { segments = raw.split("/").filter(Boolean); }
else                              { segments = []; }
```

Cloudflare Pages `[[path]]` catch-all injects `params.path` as a **string** in some runtime versions and as an **array of segments** in others. The old `path.split()` call threw `TypeError: path.split is not a function` when Pages returned an array.

All other resolver logic (alias lookup, link lookup, console telemetry, redirect) unchanged.