We confirmed that the routing entropy and curl test methodology are correct.

The observed distribution (80/20 → ~64/36 and 90/10 → ~68/32) matches an ε-greedy mixture where the exploration branch performs uniform routing.

Please patch the routing code to ensure that variant selection always uses weighted sampling.

Specifically:

1. Inspect ab-router.js and locate the ε-greedy exploration branch where uniform routing is currently performed.

It likely resembles something like:

if (explore) {
  return config.variants[bucket % config.variants.length].slug
}

2. Remove the uniform routing behavior entirely.

Exploration should NOT route uniformly. Both exploration and exploitation must fall through to the same weighted sampler.

3. Implement the canonical weighted selection algorithm:
totalWeight = sum(variant.weight)
r = random * totalWeight

cumulative = 0
for variant in variants:
cumulative += variant.weight
if r < cumulative:
return variant.slug
4. The entropy source (CF-Ray derived bucket) must remain unchanged.
We are only fixing how the bucket is mapped to a variant.

5. Do NOT change:
- experiment config schema
- API shapes
- cookie behavior
- telemetry

Only correct the weighted sampler.

Deliver a minimal patch.


PS: additionally the timer has a difficulty to take click because of the span element in the button. re-touch it and make it only the timer count 00.00.0 in the buton element