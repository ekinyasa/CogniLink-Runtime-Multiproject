export async function incrementCounter(env) {
  // KV counter logic disabled for high-traffic scalability
  // Hub metric counting is now handled exclusively by Analytics Engine
  // This stub exists to prevent breaking existing imports in router files
}
